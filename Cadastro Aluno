# SQL
CREATE TABLE ALUNO
(
    COD_ALUNO             NUMBER                NOT NULL,
    NOME                  VARCHAR2(50)         NOT NULL,
    CURSO                 VARCHAR2(50)           NOT NULL,
    SERIE                 VARCHAR2(50)           NOT NULL,
    SEXO   VARCHAR2(1) ,
    CONSTRAINT PK_COD_ALUNO PRIMARY KEY (COD_ALUNO),
    CONSTRAINT CK_SEXO_ALUNO CHECK (SEXO IN ('M','F'))
);
 
CREATE TABLE DISCIPLINA
(
    COD_DISCIPLINA        NUMBER                NOT NULL,
    TITULO                VARCHAR2(50)         NOT NULL,
    PROFESSOR             VARCHAR2(50)           NOT NULL,
    CONSTRAINT PK_COD_DISCIPLINA PRIMARY KEY (COD_DISCIPLINA)
);
 
CREATE TABLE NOTA
(
    COD_ALUNO             NUMBER                NOT NULL,
    COD_DISCIPLINA        NUMBER                NOT NULL,
    AV1                   NUMBER,
    AV2                   NUMBER,
    AV3                   NUMBER,
    AV4                   NUMBER,
    MEDIA                 NUMBER,
    RESULTADO             VARCHAR2(50),
    CONSTRAINT FK_COD_ALUNO FOREIGN KEY (COD_ALUNO) REFERENCES ALUNO (COD_ALUNO),
    CONSTRAINT FK_COD_DISCIPLINA FOREIGN KEY (COD_DISCIPLINA) REFERENCES DISCIPLINA (COD_DISCIPLINA)
);
INSERT INTO ALUNO VALUES( 1, 'ANTONIA', 'LOGICA DE PROGRAMAÇÃO', '2º SEMESTRE','F');
INSERT INTO ALUNO VALUES( 2, 'JOSÉ ALVES', 'BANCO DE DADOS', '2º SEMESTRE','M');
INSERT INTO ALUNO VALUES( 3, 'SEBASTIÃO', 'REDES', '3º SEMESTRE','M');
INSERT INTO ALUNO VALUES( 4, 'MARIA', 'PROGRAMAÇÃO PARA INTERNET', '4º SEMESTRE','F');
INSERT INTO ALUNO VALUES( 5, 'JOÃO INOCENCIO', 'LOGICA DE PROGRAMAÇÃO', '1º SEMESTRE','M');

INSERT INTO DISCIPLINA VALUES( 1, 'LOGICA DE PROGRAMAÇÃO', 'ANA MARIA');
INSERT INTO DISCIPLINA VALUES( 2, 'BANCO DE DADOS', 'CHICO CHAVEZ');
INSERT INTO DISCIPLINA VALUES( 3, 'REDES', 'SAMANTHA');
INSERT INTO DISCIPLINA VALUES( 4, 'PROGRAMAÇÃO PARA INTERNET', 'JOAO AMERICO');

INSERT INTO NOTA VALUES( 1, 1, 8, 9, 10, 1, 7, 'APROVADO');
INSERT INTO NOTA VALUES( 2, 2, 5, 4, 5, 3, 4.25, 'REPROVADO');
INSERT INTO NOTA VALUES( 3, 3, 3, 0, 0, 0, NULL, NULL);
INSERT INTO NOTA VALUES( 4, 4, 7, 4, 6, 5, 4.75, 'APROVADO');
INSERT INTO NOTA VALUES( 5, 1, 2, 4, 7, 3, 4, 'REPROVADO');
